# Optional: public absolute URL (no trailing slash) for canonical, Open Graph URL/image, and Course schema `url`.
# Set in Vercel project Environment Variables or a local `.env` file.
# VITE_PUBLIC_SITE_URL=https://your-production-host.example
